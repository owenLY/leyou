server:
  port: 6666
spring:
  application:
    name: leyou-eureka
  rabbitmq:
    host: localhost
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://127.0.0.1:${server.port}/eureka/
